# Base URL of the Django backend API
# Local dev:
NUXT_PUBLIC_API_BASE=http://localhost:8000
# Production (set in the VPS .env referenced by docker-compose's env_file):
# NUXT_PUBLIC_API_BASE=https://api.skillou.com
